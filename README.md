# CLI
Some useful command in macOS terminal.

# Usage
### List urls
```
command chrome -l
```

### Add a url
```
command chrome -a --name <name> --url <url>
```

### Remove a url
```
command chrome -d <name>
```

## License
MIT
